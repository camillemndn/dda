[package]
name = "dda"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]
name = "dda"

[profile.release]
lto = true
opt-level = 3
codegen-units = 1
panic = "abort"

[dependencies]
savvy = "0.10"
