# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.88"
name = "savvy"
version = "0.10.2"
authors = ["Hiroaki Yutani"]
build = "build.rs"
exclude = [
    "/book",
    "/R-package",
    "README.qmd",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A simple R extension interface"
readme = "README.md"
license = "MIT"
repository = "https://github.com/yutannihilation/savvy/"
resolver = "2"

[package.metadata.docs.rs]
features = [
    "complex",
    "altrep",
    "logger",
]

[package.metadata.release]
tag = true

[[package.metadata.release.pre-release-replacements]]
file = "CHANGELOG.md"
search = "Unreleased"
replace = "v{{version}}"
min = 1

[[package.metadata.release.pre-release-replacements]]
file = "CHANGELOG.md"
search = '\.\.\.HEAD'
replace = "...{{tag_name}}"
exactly = 1

[[package.metadata.release.pre-release-replacements]]
file = "CHANGELOG.md"
search = "ReleaseDate"
replace = "{{date}}"
min = 1

[[package.metadata.release.pre-release-replacements]]
file = "CHANGELOG.md"
search = "<!-- next-header -->"
replace = """
<!-- next-header -->
## [Unreleased] (ReleaseDate)
"""
exactly = 1

[[package.metadata.release.pre-release-replacements]]
file = "CHANGELOG.md"
search = "<!-- next-url -->"
replace = """
<!-- next-url -->
[Unreleased]: https://github.com/yutannihilation/savvy/compare/{{tag_name}}...HEAD"""
exactly = 1

[package.metadata.dist]
dist = false

[features]
altrep = ["savvy-ffi/altrep"]
complex = [
    "num-complex",
    "savvy-ffi/complex",
]
default = []
logger = [
    "log",
    "env_logger",
]
savvy-test = []
use-custom-error = []

[lib]
name = "savvy"
path = "src/lib.rs"

[dependencies.env_logger]
version = "0.11"
optional = true
default-features = false

[dependencies.log]
version = "0.4"
optional = true

[dependencies.num-complex]
version = "0.4.5"
optional = true

[dependencies.savvy-ffi]
version = "0.10.2"

[dependencies.savvy-macro]
version = "0.10.2"

[build-dependencies.cc]
version = "1.2.9"

[profile.dist]
lto = "thin"
inherits = "release"
