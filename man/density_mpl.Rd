% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density-mpl.R
\name{density_mpl}
\alias{density_mpl}
\title{Maximum penalized likelihood density estimation}
\usage{
density_mpl(
  x,
  WfdParobj,
  conv = 1e-04,
  iterlim = 20,
  backend = c("rust", "legacy"),
  ...
)
}
\arguments{
\item{x}{Observation vector or two-column (value, frequency) matrix.}

\item{WfdParobj}{An \code{fdPar} object (or \code{fd} / \code{basisfd}).}

\item{conv}{Convergence tolerance on the objective. Default \code{1e-4}.}

\item{iterlim}{Maximum Fisher-scoring iterations. Default \code{20}.}

\item{backend}{Which implementation to use. \code{"rust"} (default) calls the
compiled kernel via \code{\link[=density_mpl_rust]{density_mpl_rust()}}; \code{"legacy"} calls the original
R code \code{\link[=density_mpl_legacy]{density_mpl_legacy()}} for bit-for-bit compatibility.}

\item{...}{Further arguments forwarded to the selected backend.}
}
\value{
A list with \code{Wfdobj}, \code{C}, \code{Flist}, \code{iternum}, \code{iterhist}.
}
\description{
Fits a density of the form \eqn{p(x) = \exp(\phi(x)^\top c)/C} on a
bounded interval, with optional smoothness penalty.
Dispatches between the Rust backend (default, ~10–60x faster) and the
original R algorithm (preserved as \code{\link[=density_mpl_legacy]{density_mpl_legacy()}}).
}
